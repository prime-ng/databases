# Development Progress Tracker

> **Last Phase-2 deep audit (new modules):** 2026-06-21 — 10-module deep audit of newly added/graduated modules (ParentPortal, Dashboard, SchoolSetup, Library, Hostel, CommonChat, BehaviouralAssessment, Ptm, MarksheetGeneration, Feedback). Findings in `lessons/known-issues.md` section "Phase 2 — New Module Audit (2026-06-21)". Completion % updated for all 10 modules.
> **Last Phase-1 count-only re-audit:** 2026-06-21 — Full structure scan across all 45 modules. +8 modules vs 2026-04-09 (BehaviouralAssessment, CommonChat, Hostel, ParentPortal, Ptm added as new standalone modules; MarksheetGeneration and Feedback graduated from DDL-only to code). No deep code audit performed — completion % not updated for existing modules.
> **Last Phase-2 deep audit (prior):** 2026-04-09 — 6-agent parallel re-audit of modules that changed since 2026-04-02 (StudentPortal, Inventory, LmsExam/Quiz/Homework/Quests, StudentProfile Leave, Admission+Vendor, Hpc). Findings in `lessons/known-issues.md` section "Deep Audit — 2026-04-09 (Phase 2)".
> **Last count-only re-audit:** 2026-04-09 against `prime_ai` / branch `student-portal`.
> **Last full audit:** 2026-04-02 against `prime_ai` (branch current). Previous: 2026-03-22 on `prime_ai_tarun`.
> **Deep code audit:** 2026-04-02 — 8-agent parallel scan of all 37 modules. Routes→controller mapping, security, performance, validation, dead code. Results in `AI_Brain/lessons/known-issues.md` section "Deep Audit — 2026-04-02".
> **Global stats (2026-06-21 count-only):** 45 modules | 608 tenant migrations | 806 models | 747 controllers | 318 services | 3,764 views | 451 FormRequests | 8,315 module route lines | 13 jobs. +8 modules, +233 migrations, +190 models, +280 controllers, +124 services vs 2026-04-09.
> **Global stats (2026-04-09 count-only):** 37 modules | 375 tenant migrations | 616 models | 467 controllers | 194 services | 2,569 views | 288 FormRequests | 80 module-level test files | 5,384 module route lines | 8 jobs. Previous 2026-04-02 row-count sums for models/controllers/services did not match the previously stated totals (667/506/226) — the 2026-04-09 figures are the first end-to-end `find`-based re-sum.
> **Security note:** Only 1 `EnsureTenantHasModule` usage across entire tenant.php. 13 seeder routes with NO auth. 7 HPC controllers missing imports (fatal). Payment webhook behind auth (always 401). Library IS wired into tenant.php (was missing, now confirmed present).
> **Deep Gap Analysis:** Full 29-module gap analysis completed 2026-03-22. Reports in `{GAP_ANALYSIS_MODULE_WISE}/2026Mar22/`. Summary: ~950+ issues, ~140 P0 critical. Deep code audit 2026-04-02 found 14 P0 fatal, 10 P0 data-leak, 24 P1 auth-bypass, 16 P2 perf/val issues across all modules.

## V2 Requirement Documents — 2026-03-26

> **Completed:** All 46 modules + 3 summary files written to `{OLD_REPO}/2-Requirement_Module_wise/2-Detailed_Requirements/V2/`
> **Batch tracking:** `V2/_batch_progress.md` — 49 files, all 10 batches ✅ Done
> **Modes:** 24 modules FULL (read V1 + gap analysis + DDL + code) | 22 modules RBS_ONLY (greenfield)

### Key new findings from V2 generation (not in prior gap analysis)

| Module | Finding | Severity |
|--------|---------|----------|
| QNS | **Hardcoded OpenAI `sk-proj-*` + Gemini `AIzaSyD-*` API keys in source** — ROTATE IMMEDIATELY | P0 |
| PAY | Three different broken prefixes: `ptm_`, `pmt_`, and no prefix — zero DDL in tenant_db_v2.sql | P0 |
| STP | Completion revised 25%→63%; `proceedPayment` IDOR still unpatched; hard-coded dropdown ID 104 | P0 |
| HMW | `lms_homework_assignment` table missing from DDL — `publish()` crashes; `release_condition` vs `release_condition_id` mismatch | P1 |
| SLK | `bok_book_topic_mapping` table missing from DDL; `BookAuthors::index()` queries books not authors; cross-layer `Modules\Prime\Models\AcademicSession` | P1 |
| TTS | Completion revised 5%→15-20%; BUG-TTS-001: `whereIn('id')` should be `whereIn('teacher_id')` in conflict check | P1 |
| DOC | Two XSS paths: `{!! content !!}` render + JS `innerHTML` after `atob()` decode; `doc_articles` missing `sort_order` column | P1 |
| LIB | 22 controllers import `Modules\Vendor\Models\Vendor` (cross-layer); module IS in tenant.php (V1 wrong) | P1 |
| SCH | `RolePermissionController::destroy()` calls `$role->save()` not `$role->delete()` — roles never deleted | P1 |
| EXM | `dd($e)` at `LmsExamController.php:565`; 10 Gate calls commented in ExamBlueprintController; 10 in ExamScopeController | P1 |
| CMP | DDL invalid (2 type-mismatch FKs, broken index, no migrations); store() no Gate; reports/doc-requests zero auth; mobile 7/9 unguarded — see full audit 2026-06-23 | P0 |
| NTF | Gate prefix `prime.*` used instead of `tenant.*` on all notification routes | P1 |
| HPC | 5 DDL tables missing (`hpc_credit_config` etc.); FK points to `slb_circular_goals` instead of `hpc_circular_goals` | P2 |

---

## Module Completion Summary (audited 2026-03-22)

> Completion % based ONLY on what routes, models, controllers, services, and tests actually exist in code.
> Does NOT count as "complete" if controllers exist but have zero auth, empty stubs, or critical bugs.

### Central-Scoped Modules (deep-audited 2026-04-02)

| Module | Code Exists | Completion | Key Gaps (updated 2026-04-02 deep audit) |
|--------|------------|------------|----------|
| **Prime** | 22 ctrl, 27 mdl, 1 svc, 7 req | **~65%** | `is_super_admin` + `remember_token` in User $fillable; UserController passes is_super_admin via $request->only(); $request->all() in 6+ controllers despite FormRequest injected; TenantController::show()/destroy() empty; api.php apiResource points to stub; 5 controllers with 24+ stub methods |
| **GlobalMaster** | 15 ctrl, 12 mdl, 0 svc, 10 req | **~50%** | `$request->all()` in 12 places; GlobalMasterController/OrganizationController all stubs; LanguageController uses `global-master.*` permission prefix (mismatch with seeded); N+1 in DropdownController::index(); AcademicSessionController::destroy() condition logically inverted; double activityLog() in State/Module controllers |
| **SystemConfig** | 4 ctrl, 3 mdl, 0 svc, 1 req | **~40%** | ZERO auth on SystemConfigController (7 methods); MenuSyncController auth check COMMENTED OUT (any user truncates menus); MenuController 5 stubs; SettingController returns raw $request object; wasted Setting::all() query |
| **Billing** | 7 ctrl, 6 mdl, 0 svc, 3 req, 1 job | **~55%** | **No try/catch on DB transaction in payment processing** — failures corrupt invoice state; both store() and consolidatedStore() affected; BillingManagementController::store() no ownership check on planRateId; Tenant::get()+User::get() unbounded on every page; 15+ stub methods; printData() calls isNotEmpty() on float (crash) |
| **Documentation** | 3 ctrl, 2 mdl, 0 svc, 2 req | **~60%** | Gate permission mismatch (.store vs .create); XSS risk on Summernote; 20MB upload limit excessive; DocumentationController (only routed one) is mostly stubs; Article/Category controllers well-built but unrouted |

### Tenant-Scoped Modules — School Administration (deep-audited 2026-04-02)

| Module | Code Exists | Completion | Key Gaps (updated 2026-04-02 deep audit) |
|--------|------------|------------|----------|
| **SchoolSetup** | 61 ctrl, 42 mdl, 0 svc, 31 req | **~35%** (2026-06-21 deep audit, ↓5 from prior) | is_super_admin writable via UserController + in User $fillable + in UserRequest; SchoolSetupController all stubs; **NEW DEEP AUDIT 2026-06-21:** 5 routed methods missing in EmployeeProfileController (→ 500), 3 in OrganizationAcademicSessionController, 2 in ClassGroupController, 1 each in RolePermissionController/TeacherController/SubjectStudyFormatController (SEC-SCH-017, BUG-SCH-017 through BUG-SCH-024). `OrganizationAcademicSessionController::setActiveSession()` has zero Gate::authorize — switches org-wide active session with no auth (SEC-SCH-017 P0). `ClassSubjectManagementController` and `SchoolSetupController` CRUD methods are empty stubs — data never persisted or deleted. All 31 FormRequest `authorize()` return bare true (SEC-SCH-020). N+1 in `ClassGroupController::generateClassGroups()` — with() call commented out (PERF-SCH-001). 5 versioned backup controller files + `competency.blade.php` in Controllers dir + commented-out dd()/print_r() calls (DEAD-SCH-001/002/003). **Employee Leave DDL (v4.sql, D33):** 25 tables — code not yet implemented. **PTM DDL v2 (D35):** 10 `ptm_*` tables — moved to standalone Ptm module. Total findings: 1 SEC P0, 3 SEC P1, 8 BUG P1, 1 PERF P1, 2 VAL P2, 3 DEAD P2. |
| **Feedback (NEW — 2026-04-09)** | 10 ctrl, 11 mdl, 6 svc, 1 req | **~28%** (2026-06-21 deep audit — see "Previously DDL-Only Modules" section below for full findings) | Dedicated module `39-Feedback/` with `fbk_*` prefix (11 tenant tables). DDL v3 current. **Code shipped 2026-06-21: 10 ctrl, 11 mdl, 6 svc, 1 req.** DEEP AUDIT COMPLETE — see "Previously DDL-Only Modules" row for full findings. Summary: dual P0 (zero Gate in 9/10 controllers + eligibility service never called); entire API dead; FbkSummaryController class missing. |
| **StudentProfile** | 5 ctrl, 18 mdl, 1 svc, 0 req | **~20%** (2026-04-09 Phase 2, ↓12 from ~32%) | is_super_admin writable in student login creation; **Gate facade not imported in AttendanceController** — all Gate calls fatal; StudentReportController zero auth on PII reports; module web.php registers only stub controller; storeBulkAttendance validation fully commented out; 0 FormRequests. **Leave subsystem verified DEAD CODE (2026-04-09 Phase 2):** 4 Leave* models + LeaveService exist with real logic but NO LeaveController, ZERO routes registered. Feature unreachable from UI/API — Phase 1's +4 models/+1 svc counted file existence only. `markAttendanceOnApproval()` is a "to be implemented" stub (BUG-STD-001). No tenant scoping on LeaveApplication/Remark (SEC-STD-005). Half-day overlap fraud vector (VAL-STD-002). % dropped to ~20% since Leave files add zero functional surface. |
| **Transport** | 31 ctrl, 36 mdl, 0 svc, 18 req | **~40%** | Module web.php registers 0 tenant routes; `tested.*` permission typo blocks AttendanceDeviceController; updateLastSeen() publicly accessible (no auth); **Aadhaar/PAN stored plaintext** (IT Act violation); TptVehicleServiceRequestController zero auth + zero validation; 3 different permission prefixes (tenant.*/tested.*/transport.*) |
| **StudentFee** | 15 ctrl, 23 mdl, 0 svc, 0 req | **~50%** | 14 seeder methods (~1,200 lines) in production controller; StudentFeeController zero Gate auth; module web.php registers only stub; FeeInvoiceController IDOR (student_assignment_id unchecked); N+1 in bulk invoice generation; 0 FormRequests for financial data |
| **Vendor** | 8 ctrl, 8 mdl, 0 svc, 3 req, 1 job | **~50%** (2026-04-09 Phase 2, ↓3) | **BUG-VND-002 CONFIRMED STILL UNPATCHED (2026-04-09 Phase 2):** VendorPaymentController create/store/edit methods still missing → Resource routes return 500. **SEC-VND-005 CONFIRMED:** VendorController::index() Gate::authorize still commented out (VendorController.php:26). **SEC-VND-006 CONFIRMED:** Gate prefix mismatch still present — VndUsageLogController uses `vendor.usageLog.*`; VendorPaymentController uses `vendor.vendor.viewAny`; other controllers use `tenant.vendor-*`. Policies under `tenant.*` never match → silent 403s. **RESOLVED (good news):** Vendor RouteServiceProvider now includes full tenancy middleware stack (InitializeTenancyByDomain/PreventAccessFromCentralDomains/EnsureTenantIsActive) per post-D22 migration. **NEW controller:** VendorReportController added 2026-04-06 with proper `tenant.vendor-report.viewAny` Gate — compliant. |
| **Complaint** | 10 ctrl, 6 mdl, 2 svc, 2 req | **~20%** (2026-06-23 full re-audit ↓10) | **P0 (fix before testing):** DDL invalid — 2 type-mismatch FKs crash CREATE TABLE (SCH-CMP-002/003); no tenant migrations (SCH-CMP-007/DEPLOY-CMP-01); `ComplaintController::store()` has NO Gate::authorize — any tenant user creates complaints (SEC-CMP-007); `DocumentRequestController` zero auth all 3 methods (SEC-CMP-008); `AiInsightController` complete stub on live routes zero auth (DEAD-CMP-001). **P1:** Broken index references non-existent `status` column (SCH-CMP-001); column name mismatch code vs DDL `expected_resolution_hours` vs `default_expected_resolution_hours` — all category escalation queries return null (SCH-CMP-004); `complaints/manage` permanently shadowed by resource show route (BUG-CMP-015); ComplaintController missing 4 methods trashed/restore/forceDelete/toggleStatus → 500 (BUG-CMP-016); api.php maps web controller as apiResource — 7 API routes return HTML (BUG-CMP-017); Mobile API zero Gate on 7/9 methods dashboard/index/show/users (SEC-CMP-010); ComplaintActionController wrong permission prefix `complaint.complaint.*` (SEC-CMP-011); Cross-module dependency: Complaint imports ParentPortal models (SEC-CMP-009); `ComplaintReportController` zero auth on all methods (SEC-CMP-006 — STILL UNRESOLVED). **P2:** `sys_dropdown_table` FK vs `sys_dropdowns` code mismatch (SCH-CMP-006); dummy_table_name dropdown keys in MedicalCheckController (BUG-CMP-013 — STILL UNRESOLVED); 8 N+1/unbounded perf issues; schema introspection in hot path (PERF-CMP-001–008). **RESOLVED:** RSP now has full tenancy middleware (web routes). **Audit report:** `3-Audit_Modules/V1_22Jun2026/Complaint/Complaint_Audit_2026-06-23.md` |
| **Notification** | 12 ctrl, 14 mdl, 2 svc, 10 req | **~35%** | **ALL controllers use `prime.*` Gate prefix** — every tenant check returns 403; **Tenant::all() in tenant context** → cross-tenant data leak; 2 controllers don't exist on disk; routes partially commented out; $request->all() mass-assigned |
| **Payment** | 2 ctrl, 5 mdl, 2 svc, 1 req | **~45%** | **Webhook behind auth middleware** — all gateway callbacks return 401; payable_type accepts arbitrary class name (no allowlist); duplicate index routes |
| **Dashboard** | 26 ctrl, 0 mdl, 0 svc, 0 req | **~30%** (2026-06-21 deep audit, ↑5 from count only; real completion low due to dummy data) | **NEW DEEP AUDIT 2026-06-21:** Zero role-level authorization across all 26 controllers — auth+verified middleware only; `/dashboard/superadmin` exposes tenant list, billing, error logs, security audit to any authenticated user (SEC-DSH-006, SEC-DSH-007). **8 of 26 controllers serve entirely hardcoded dummy data** (AccountsDashboard, Inventory, Library, Principal, Teacher, SuperAdmin, Management, Transport) returning fabricated student names and balances in production (DEAD-DSH-001). API `apiResource` registers 6 unimplemented methods on DashboardController — all crash at runtime (BUG-DSH-006). `BaseDashboardController::safeCount()` fires uncached `Schema::getColumnListing` on every invocation — 24× per main dashboard page (PERF-DSH-001). Findings: 2 SEC P1, 1 BUG P1, 2 PERF P2, 1 DEAD P1. |
| **Scheduler** | 1 ctrl, 2 mdl, 2 svc, 1 req | **~25%** | **No tenancy middleware in RSP** — runs on wrong database; zero Gate auth; store() redirects to non-existent route; show/edit/update/destroy stubs; cron_expression no format validation |
| **StudentPortal** | 14 ctrl, 11 mdl, 1 svc, 5 req | **~50%** (2026-04-09 Phase 2, ↓18 from ~68%) | Substantial growth since 2026-04-02 (was 7/0/0/0). **2026-04-09 Phase 2 deep audit findings:** **P0 SEC-STP-007 CONFIRMED UNPATCHED** — IDOR in `proceedPayment()` at StudentPortalController.php:427-450 (any student pays another's invoice). **P0 SEC-STP-008** — IDOR in `StudentExamAttemptController::attempt($id)` — no allocation check (attempt flow bypasses instructions flow gate). **P1 SEC-STP-009** — `User::all()` exposes full tenant user roster in complaint form (StudentPortalComplaintController.php:41). **Validation gap:** 9 of 14 controllers have NO FormRequest injection (VAL-STP-001). **Dead code:** ParentResultsController has 2 methods but NO routes; StudentPortalController has 7 empty CRUD stubs (lines 711-759); StudentPortalComplaintController show/edit/update/destroy are stubs. **Perf:** `dashboard()` fires 5+ queries with N+1 on homework; `loadExamQuestions()` N+1 on options. **Mass assignment:** ExamAttempt + ExamGrievance have `created_by` / `reviewer_id` in $fillable. **% dropped to ~50%** because new controllers shipped without security hardening, validation, or routing of scaffolded features. **Prompt:** `5-Work-In-Progress/StudentPortal/1-Claude_Prompt/STP_2step_Prompt1.md` |

### Tenant-Scoped Modules — Academic & Curriculum (deep-audited 2026-04-02)

| Module | Code Exists | Completion | Key Gaps (updated 2026-04-02 deep audit) |
|--------|------------|------------|----------|
| **Syllabus** | 15 ctrl, 22 mdl, 1 svc, 14 req | **~55%** | 14 controllers not routed (only SyllabusController has routes); CompetencieController 6 methods zero Gate + $request->all(); Competencie model missing SoftDeletes; N+1 in getCompetencyTree() recursive children; getLevelLabel() fires DB query per tree node; TopicCompetencyController::update() no validation; wrong permission names (tenant.subject.* used on non-subject controllers) |
| **SyllabusBooks** | 4 ctrl, 6 mdl, 0 svc, 3 req | **~55%** | **Only routed controller has empty store/update/destroy** — routes do nothing; BookTopicMappingController::index() crashes (undefined variable); AuthorController::store()/update() missing Gate; **Cross-layer: BookController queries Prime\AcademicSession from tenant context** |
| **QuestionBank** | 7 ctrl, 17 mdl, 0 svc, 6 req | **~45%** | API keys FIXED (now env()); **generateQuestions() permanently returns demo data** — real AI path unreachable dead code; AIQuestionGeneratorController entire controller zero auth; startImport() missing Gate; 6 controllers unrouted |
| **LmsExam** | 13 ctrl, 11 mdl, 1 svc, 11 req | **~55%** (2026-04-09 Phase 2, ↓3) | **FIXED:** `dd($e)` removed from LmsExamController::store() (DEAD-EXM-001 verified fixed 2026-04-09). **STILL PRESENT:** BUG-EXM-003 — ExamStudentGroupMemberController::toggleStatus() missing → 500 at routes/web.php:130. **NEW P0 SEC-EXM-005:** GrievanceReviewController::show() + resolve() have ZERO Gate::authorize — any user can read/resolve any exam grievance (IDOR). **SEC-EXM-006:** PaperSetQuestionRequest::authorize() returns hardcoded true (mitigated by controller gates but no defense-in-depth). **SEC-EXM-007:** ExamQueryService (381 lines) lacks explicit tenant scoping — used by both new controllers. **PERF-LMS-002:** 9+ unbounded queries in LmsExamController::index() CONFIRMED STILL PRESENT. PaperSetQuestionController (15+ methods) has proper Gate checks — COMPLIANT. |
| **LmsQuiz** | 6 ctrl, 6 mdl, 1 svc, 5 req | **~72%** | Gate calls NOW active on all methods (BUG-LMS-005 FIXED for Quiz); route prefix typo `lms-quize`; store() no DB transaction; unused `$original = clone $quiz`; student attempt tracking absent. **NEW (2026-04-09 count):** +1 controller, +1 service (files present, not re-audited). |
| **LmsHomework** | 2 ctrl, 3 mdl, 1 svc, 3 req | **~52%** | HoemworkData $request param FIXED; review() Gate FIXED; **P0 SEC-HWK-003 CONFIRMED 2026-04-09: show() has no auth (IDOR)** — HomeworkSubmissionController.php:225-229; 4 unused imports; unbounded Student::get() and HomeworkAssignment::get() in 3 methods; student submission portal missing. HomeworkQueryService (new) contains real logic. |
| **LmsQuests** | 4 ctrl, 4 mdl, 1 svc, 4 req | **~52%** | **SEC-QZT-002 CONFIRMED 2026-04-09:** Gate still commented in `LmsQuestController::index()` (BUG-LMS-005 STILL PARTIAL). **BUG-QST-TOPICS FIXED** — getTopics() method now exists. No DB transactions on writes; student quest player absent. QuestQueryService (new) contains real logic. |
| **Recommendation** | 10 ctrl, 11 mdl, 0 svc, 0 req | **~39%** | **Gate::any() without abort(403)** — auth checks don't block; **StudentRecommendationController uses .create for delete/restore/forceDelete**; show()/edit() wrong permission; uses App\Models\User (wrong namespace); store/update/destroy empty stubs; 4 inconsistent permission prefixes |

### Tenant-Scoped Modules — Timetable & HPC (deep-audited 2026-04-02)

| Module | Code Exists | Completion | Key Gaps (updated 2026-04-02 deep audit) |
|--------|------------|------------|----------|
| **SmartTimetable** | 19 ctrl, 65 mdl, 108 svc, 7 req, 177 views, 14 seeders | **~58%** | **generateForClassSection missing → 500**; API controller zero Gate; ParallelGroupController bypasses tenancy stack; SubstitutionService crash (undefined $candidates) + Carbon API misuse (now()->parse()); God controller 3,501 lines + 8 unused imports; createConstraintManager() all 12 constraints commented out; index() fires 14+ unbounded queries; viewAndRefinement() loads all cells unbounded; 0 tests. **Full doc:** `5-Work-In-Progress/2-In-Progress/SmartTimetable/SmartTimetable_Module_Documentation.md` |
| **TimetableFoundation** | 24 ctrl, 32 mdl, 3 svc, 4 req | **~65%** | **2 routes → missing methods (generateAllActivities, getBatchGenerationProgress) → 500**; generateActivities runs 400+ updateOrCreate in web request; ClassWorkingDayController up to 10,000 upserts in single request; EnsureTenantHasModule missing; 20/24 controllers use inline validation; eligible_teacher_count hardcoded to 1 |
| **StandardTimetable** | 1 ctrl, 0 mdl, 0 svc, 0 req | **~5%** | Module skeleton — **controller file doesn't exist** despite being imported in tenant.php (fatal) |
| **Hpc** | 23 ctrl, 32 mdl, 10 svc, 14 req | **~57%** (2026-04-09 Phase 2, ↓2) | **SEC-RTG-005 PARTIALLY RESOLVED:** All 23 controllers now properly imported in `Modules/Hpc/routes/web.php` post-D22 migration — route-layer class-not-found risk is gone. **NEW BUG-HPC-016 (regression):** `HpcController::generateReportPdf()` at line 1232 is **MISSING `Gate::authorize()`** — inconsistent with 13 sibling methods that all have auth (per 2026-03-17 sprint). Any authenticated user can generate PDF reports for any student. **SEC-HPC-001 through 005 STILL FIXED** for 13/14 HpcController methods and all 14 FormRequests (sprint fixes hold). 10 HPC policies correctly registered in HpcServiceProvider::registerPolicies(). |
| **Library** | 39 ctrl, 35 mdl, 9 svc, 27 req | **~42%** (2026-06-21 deep audit, ↓3 from prior) | IS wired into tenant.php (confirmed present at lines 2770+); **NEW DEEP AUDIT 2026-06-21:** **9 routed methods missing from controllers** — all 5 LibReportPrintController print methods, LibFineReportController export/refresh, LibFineController testAccounting, LibReservationController cancelPage — all throw 500 in production (BUG-LIB-010 P0). `LibraryController::store()`, `update()`, `destroy()` are empty stubs wired to live API routes (BUG-LIB-011). `StaffLibraryController` has zero Gate::authorize across all 5 write methods — any authenticated user can reserve/cancel/review (SEC-LIB-010 P1). All 27 FormRequests return bare true (SEC-LIB-011). N+1 INSERT in bulkStore loops in LibInventoryAuditDetailController and LibFineSlabConfigController (PERF-LIB-010). `LibInventoryAuditController::update()` accepts unvalidated `$request->details` array with no type/structure check (VAL-LIB-001). ~260 lines of commented-out dead code in LibInventoryAuditController (DEAD-LIB-002). Commented dd() with Hindi debug comment in LibAuthorController and MasterDashboardController (DEAD-LIB-001). Total findings: 1 BUG P0, 2 SEC P1, 1 BUG P1, 1 PERF P1, 2 VAL P2, 2 DEAD P2. |

### Accounting & Supporting Modules (deep-audited 2026-04-02)

| Module | Code Exists | Completion | Key Gaps (updated 2026-04-02 deep audit) |
|--------|------------|------------|----------|
| **Accounting** | 18 ctrl, 21 mdl, 0 svc, 15 req, 14 tests | **~40%** | Scaffolding 85% done but core operations 30%: matchEntry/unmatchEntry route wildcard mismatch → 500; runDepreciation missing {fixed_asset} in route → 500; ExpenseClaim race condition (count+1); Dashboard 6 unbounded queries + 12-query monthly loop; autoMatch/importStatement/postNow/runDepreciation are stubs returning false positives |

### Newly Scaffolded Modules (deep-audited 2026-04-02) — Scaffold Done, Partial Implementation

> % based on file counts + business logic verification. ~15% = scaffold + stubs. ~20% = scaffold + some real logic.

| Module | Code Exists | Completion | Notes (updated 2026-04-02 deep audit) |
|--------|------------|------------|-------|
| **Admission** | 18 ctrl, 20 mdl, 6 svc, 24 req, 84 views, 259 route lines | **~35%** (2026-04-09 Phase 2, ↑15; counts updated 2026-06-21) | **MAJOR IMPROVEMENT verified 2026-04-09:** All 15 controllers now have explicit `Gate::authorize()` calls — service-layer-only auth replaced with defense-in-depth. All 3 new FormRequests (StoreBehaviorActionRequest, StoreBehaviorIncidentRequest, UpdateBehaviorIncidentRequest) properly DI-injected into AlumniController — none orphaned. EnquiryController::index() + ApplicationController::index() use proper `with()` eager loading — no N+1. AdmissionPipelineService has real FSM+fee logic. **Only remaining issue:** BUG-ADM-003 — AdmissionPipelineService.php:73 still references `$application->cycle_id` (old attribute) — missed from the cycle_id→admission_cycle_id rename commits (1bbf9d56e, ae38c3c4e). All 15 controllers routed, no unrouted controllers. Prompt ready. |
| **Cafeteria** | 16 ctrl, 21 mdl, 6 svc, 19 req, 95 views, 189 route lines | **~18%** | OrderController has real logic via OrderService. **Student IDOR in apiIndex()** — student_id from query string unverified. No Gate. Prompt ready. |
| **Certificate** | 10 ctrl, 10 mdl, 3 svc, 10 req, 39 views, 1 job, 142 route lines | **~15%** | Scaffold stubs only. No real implementation. Prompt ready. |
| **FrontOffice** | 21 ctrl, 22 mdl, 4 svc, 10 req, 118 views, 1 job, 310 route lines | **~12%** | Scaffold stubs only. Only 3 FormRequests for 20 controllers. Prompt ready. |
| **HrStaff** | 22 ctrl, 33 mdl, 15 svc, 23 req, 93 views, 242 route lines | **~20%** | PayrollController well-implemented with Gate + FormRequests + service layer. **Arbitrary column override risk via field_name** in override(). Prompt ready. |
| **Inventory** | 20 ctrl, 28 mdl, 14 svc, 18 req, 77 views, 1 job, 229 route lines | **~22%** | Services doubled since 2026-04-02 (7→14) — added Asset, Godown, GrnPosting, InventoryReport, PurchaseOrder, PurchaseRequisition, Quotation, RateContract, ReorderAlert, StockAdjustment, StockGroup, StockIssue, StockLedger, StockValuation. +5 FormRequests, +17 views, +36 route lines. Deep audit of new services not yet run. Prompt ready. |
| **EventEngine** | 4 ctrl, 3 mdl, 0 svc, 3 req, 17 views, 16 route lines | **~20%** | 3 CRUD sub-controllers well-implemented with Gate. **EventEngineController zero auth**; **No tenancy middleware in RSP** — runs on wrong DB; rule firing engine doesn't exist (config UI only); isset() vs filled() filter bug |

### Previously DDL-Only Modules — Now Have Code (graduated 2026-06-21)

| Module | Code Exists | Completion | Notes |
|--------|------------|------------|-------|
| **MarksheetGeneration** | 21 ctrl, 24 mdl, 33 svc, 19 req, 98 views, 1 job, 180 route lines | **~52%** (2026-06-21 deep audit) | DDL-only as of 2026-04-09. **DEEP AUDIT 2026-06-21:** Solid Gate authorization coverage across nearly all controllers. **Critical bugs:** entire API layer is wired to a controller with zero apiResource methods — instant 404 on all API calls (BUG-MSG-001 P0). `StudentSubjectResultController` missing `destroy()` method despite resource route registering it (BUG-MSG-002). `ExamGroupController::edit()` missing model binding parameter — edit form unusable (BUG-MSG-003). **SEC:** `StudentResultController::create()` uses wrong Gate ability (`view` instead of `create`) and `store()` uses `update` ability (SEC-MSG-001/002 P1). All 19 FormRequests return bare true (SEC-MSG-003). **PERF:** `precheck()` fires 6 DB queries per class in a foreach — 120+ queries for 20-class school (PERF-MSG-001). Unbounded `Student::get()` in create/edit forms (PERF-MSG-003). Total findings: 1 BUG P0, 3 BUG P1, 2 SEC P1, 1 SEC across all FRs, 3 PERF P1/P2, 1 VAL P2, 1 DEAD P2. |
| **Feedback** | 10 ctrl, 11 mdl, 6 svc, 1 req, 51 views, 171 route lines | **~28%** (2026-06-21 deep audit) | DDL-only as of 2026-04-09. **DEEP AUDIT 2026-06-21:** Worst module in this batch. **Dual P0:** zero Gate::authorize in 9 of 10 controllers — any authenticated tenant user can create/update/delete cycles, templates, and setup entities (SEC-FBK-001). Injected `FbkEligibilityService` is never called in `saveDraft()`/`submit()` — eligibility gate completely bypassed (SEC-FBK-002). Entire API surface (api.php) is dead — RouteServiceProvider never registers it (BUG-FBK-001). `FbkSummaryController` imported in api.php but class file does not exist — fatal on registration (BUG-FBK-002). `submit()`/`saveDraft()` accept unvalidated answer payloads directly to DB (VAL-FBK-001). 9 store/update methods use plain `Request` with some having no `validate()` call at all (VAL-FBK-002). N+1 in `FbkSummaryService::batchRecomputeForCycle()` (PERF-FBK-001). `FbkEligibilityService` injected but never referenced — dead weight instantiated on every request (DEAD-FBK-001). Total findings: 2 SEC P0, 2 BUG P1, 1 VAL P1, 2 VAL P2, 1 PERF P2, 1 DEAD P2. |

### Newly Added Modules (first detected 2026-06-21)

> All 5 have `module.json` without `enabled` field — registered via providers but not yet enabled in `modules_statuses.json`. Deep code audit pending for all.

| Module | Code Exists | Completion | Notes |
|--------|------------|------------|-------|
| **Hostel** | 53 ctrl, 44 mdl, 22 svc, 38 req, 278 views, 2 jobs, 573 route lines | **~58%** (2026-06-21 deep audit) | Fully scaffolded. HST DDL v3 (36 tables). Prefix: `hst_*`. **DEEP AUDIT 2026-06-21:** **4 completely missing controller methods wired to live routes** — `VisitorLogController::storeMedia/destroyMedia`, `MessOptOutController::approve/reject`, `MessBillController::publish`, `RoomReservationController::confirm/cancel` — all throw fatal 500 (BUG-HST-001 through 004 P0). `AuditLogController::destroy` commented out but auto-generated by resource route (BUG-HST-005). **6 report controllers have zero Gate::authorize** — HostelMedicalReportController exposes sick-bay health data, HostelDisciplineReportController exposes discipline histories, plus 5 others (SEC-HST-001/002/003 P1). All 38 FormRequests return bare true (SEC-HST-002 P2). N+1 in `HostelOccupancyReportService::getFloorPlan()` — 400+ queries per page for large hostels (PERF-HST-001 P1). 13 unconditional service calls in `HostelAttendanceReportController::index()` (PERF-HST-002). 3 controllers use inline validate instead of FormRequest (VAL-HST-001). 50+ lines of commented-out code in AuditLogController (DEAD-HST-001). Total findings: 4 BUG P0, 1 BUG P1, 3 SEC P1, 1 PERF P1, 2 PERF P2, 1 VAL P2, 1 DEAD P2. **Prompt:** `5-Work-In-Progress/Hostel/1-Claude_Prompt/HST_2step_Prompt1.md` |
| **ParentPortal** | 28 ctrl, 6 mdl, 1 svc, 0 req, 45 views, 267 route lines | **~45%** (2026-06-21 deep audit) | **DEEP AUDIT 2026-06-21:** **P0: `Gate::define()` permanently overwrites `tenant.hpc.view` for the entire PHP-FPM worker lifetime** in `ParentResultController::reportCardPdf()` — privilege escalation vector for all subsequent gate checks in the same process (SEC-PPT-001). **P0: PTM `book()` has no class/section scope guard** on slot — cross-class slot booking IDOR (SEC-PPT-003). **P0: Complaint `store()` writes 4 unvalidated user-supplied fields directly to DB** (SEC-PPT-004). **P0: Zero FormRequest classes** across all 28 controllers (SEC-PPT-002/VAL-PPT-001). 4 API controllers are abort(501) stubs wired to live routes (BUG-PPT-001). `ParentPortalController` scaffold stub with 3 empty method bodies (BUG-PPT-002). 3 PERF issues including N+1 on notification preferences and 5 separate COUNT queries for leave status. Total findings: 4 SEC P0, 2 SEC P1, 3 BUG P1, 3 PERF P2, 2 VAL P1/P2, 1 DEAD P2. **Prompt:** `5-Work-In-Progress/ParentPortal/1-Claude_Prompt/PPT_2step_Prompt1.md` |
| **BehaviouralAssessment** | 12 ctrl, 16 mdl, 0 svc, 5 req, 65 views, 119 route lines | **~58%** (2026-06-21 deep audit) | **DEEP AUDIT 2026-06-21:** Functionally solid for primary domain controllers with consistent Gate::authorize(). **Critical gaps:** `BehaviouralAssessmentController::store()/update()/destroy()` are empty stubs wired to live API apiResource (BUG-BEH-001 P0). **No auth middleware on web.php** — 100+ routes unguarded at route layer; Gate::authorize in controllers is the only protection (SEC-BEH-002 P1). All 5 FormRequests return bare true (SEC-BEH-001). `BaReportController::export()` permanently returns HTTP 501 (BUG-BEH-002). N+1 in `BaIncidentController::show()` (one DB query per witness) and `ratingDetailMapQuery` (missing `criterion.category` in with()) (PERF-BEH-001/002 P1). `bulkRate()` fires 600 queries for 30 students × 10 criteria (PERF-BEH-003 P2). 2 controllers use inline validate instead of FormRequest (VAL-BEH-001/002). Entire `BehaviouralAssessmentController` is dead scaffold behind live API route (DEAD-BEH-001). Total findings: 1 BUG P0, 2 SEC P1, 1 BUG P1, 2 PERF P1, 2 PERF P2, 2 VAL P2, 1 DEAD P2. |
| **CommonChat** | 15 ctrl, 9 mdl, 1 svc, 5 req, 19 views, 121 route lines | **~48%** (2026-06-21 deep audit) | **DEEP AUDIT 2026-06-21:** Core web chat (ChatController, ChatMessageController, ChatParticipantController) well-structured with consistent Gate::authorize() and proper middleware. **`ChatAjaxController` (13 routes, serving web widget and SPA) has zero Gate/Policy checks** — `createConversation` and others skip participant membership verification (SEC-CCH-001 P1). `CommonChatController::store()/update()/destroy()` are empty stubs wired to live apiResource (BUG-CCH-001 P1). 3 controllers with no route registrations (DEAD-CCH-001). N+1: `messages()` fires 2 DB queries per message (receipts counts), `conversations()` fires 2 queries per row (PERF-CCH-001/002 P1). `ChatSettings::first()` called twice in `sendMessage()` with no memoization (PERF-CCH-003 P2). `UpdateParticipantRequest`/`MarkAsReadRequest` return bare true (SEC-CCH-002 P2). `createConversation` uses inline validate instead of FormRequest (VAL-CCH-001). Total findings: 1 SEC P1, 1 BUG P1, 2 PERF P1, 1 PERF P2, 1 SEC P2, 1 VAL P2, 1 DEAD P1. |
| **Ptm** | 11 ctrl, 9 mdl, 6 svc, 18 req, 58 views, 103 route lines | **~55%** (2026-06-21 deep audit) | **DEEP AUDIT 2026-06-21:** Solid route-to-controller coverage (no missing methods), FormRequests cover all store/update paths. **P0: `PtmManagementController` AJAX endpoints (getEventTeachers, getEventStudents, getTeacherSlots) and `PtmCombinedViewController` views have zero Gate checks** — any authenticated tenant user can access sensitive PTM data (SEC-PTM-001). All 18 FormRequests return bare true (SEC-PTM-002 P1). `PtmCombinedViewController::scheduling()` is orphaned dead code with no route (DEAD-PTM-001 P1). **3 confirmed N+1 / performance issues:** management dashboard fires 14+ uncached queries per load including 8 separate `onlyTrashed()->count()` calls (PERF-PTM-001); `PtmBlockoutService::notifyAffectedBookings()` discards eager-loaded guardians and re-queries per booking (PERF-PTM-002); `PtmSlotService::syncSlotStatusForBlockout()` fires one blockout existence query per slot (PERF-PTM-003). Duplicate 5 unbounded queries between `setup()` and `sharedData()` (PERF-PTM-004). `cancel()` and `reschedule()` use inline validate (VAL-PTM-001). Total findings: 1 SEC P0, 1 SEC P1, 3 PERF P1, 1 PERF P2, 1 VAL P2, 1 DEAD P1. |

---

## Requirements Complete — Development Pending

- [x] **HRS — HR & Payroll** (0% code, module not created) — Planning complete 2026-03-26. `Modules\HrStaff`. Dual prefix: `hrs_*` (23 tables) + `pay_*` (10 tables). Payroll merged into HrStaff (v2 decision). Outputs: `HRS_FeatureSpec.md`, `HRS_DDL_v1.sql` + migration + 7 seeders, `HRS_Dev_Plan.md`. 20 controllers, 15 services, 30 FormRequests, ~110 views, 102 routes, 6 implementation phases (Sprint 1–12), 22 feature tests + 7 unit test files planned.
- [x] **Inventory** (0% code, module not created) — Prompt ready 2026-03-26. `Modules\Inventory`. Prefix: `inv_*` (28 tables). 18 controllers, 7 services, 13 FormRequests, ~65 views, ~65 routes, 4 seeders + runner, 10 implementation layers. Prompt: `5-Work-In-Progress/22-Inventory/1-Claude_Prompt/INV_2step_Prompt1.md`.
- [x] **FrontOffice / FOF** (0% code, module not created) — Prompt ready 2026-03-26. `Modules\FrontOffice`. Prefix: `fof_*` (22 tables). 16 controllers, 5 services (VisitorService, GatePassService, CircularService, CertificateIssuanceService, EarlyDepartureService), ~75 web routes, ~12 API routes. Key: DomPDF certificates, ATT sync (EarlyDepartureAttSyncJob — 3 retries), public feedback token URL (no auth), fof:flag-overstay Artisan daily. Prompt: `5-Work-In-Progress/FrontOffice/1-Claude_Prompt/FOF_2step_Prompt1.md`.
- [x] **AdmissionMgmt / ADM** (0% code, module not created) — Prompt ready 2026-03-26. `Modules\Admission`. Prefix: `adm_*` (20 tables). 14 controllers, 6 services (AdmissionPipelineService, MeritListService, EnrollmentService, TransferCertificateService, PromotionService, AdmissionAnalyticsService), ~65 web routes, ~20 API routes. Key: atomic enrollment DB::transaction (sys_users + std_students + std_student_academic_sessions), payment webhook outside auth middleware (signature-verified only, idempotent), NEP 2020 entrance test compliance, QR-code TC. Prompt: `5-Work-In-Progress/FrontOffice/1-Claude_Prompt/ADM_2step_Prompt1.md`.
- [x] **Hostel / HST** (0% code, module not created) — Prompt ready 2026-03-26. `Modules\Hostel`. Prefix: `hst_*`. 20 controllers, 7 services (AllotmentService, LeavePassService, HstAttendanceService, IncidentService, HostelFeeService, HstComplaintService, SickBayService), ~65 routes. Key: dual generated-column UNIQUE on hst_allotments (active-bed + active-student), leave approval in DB::transaction (3 ops atomic), WardenScopeMiddleware, hst_sick_bay_log.hpc_record_id soft FK (no constraint), HostelFeeService service-to-service only (no FK to fin_*), DomPDF (leave pass PDF + warning letter). Prompt: `5-Work-In-Progress/Hostel/1-Claude_Prompt/HST_2step_Prompt1.md`. **DDL EVOLUTION:** v2 = 21 tables. **v3 (2026-05-04, D34) = 36 tables** at `1-DDL_Tenant_Modules/Hostel/DDL/HST_DDL_v3.sql` — added `created_by`/`updated_by` on every v2 table (was systematically missing despite v2's own header promising them); added 15 new tables across 10 domains (warden duty roster, bed maintenance log, housekeeping log, laundry tickets, mess opt-outs, mess bills, fee demands, incident types master, incident warnings, room reservations, hostel emergency contacts, visitor media, sick-bay vitals, sick-bay medications, generic audit log, notification log); added 14 fields to existing v2 tables. Strictly additive — no renames. Code not yet implemented.
- [x] **StudentPortal / STP** (~55% code, existing module) — Completion prompt ready 2026-03-26. `Modules\StudentPortal`. Prefix: stp_* (ZERO owned tables — reads 30+ tables from 15 modules). 7 existing controllers (1,317 lines), 57 views, 35 screens, 55+ routes, 0 services/FormRequests/policies. P0: IDOR in proceedPayment + EnsureTenantHasModule missing + zero Gate calls. P1: hard-coded dropdown ID 104, PaymentGateway::all(), currentFeeAssignemnt typo, mark-read GET→POST. Phase 2 of prompt = Security & Architecture (FormRequests + Policy + Service) NOT DDL. ~15 person-days to complete. Prompt: `5-Work-In-Progress/StudentPortal/1-Claude_Prompt/STP_2step_Prompt1.md`.
- [x] **Cafeteria / CAF** (0% code, module not created) — Prompt ready 2026-03-26. `Modules\Cafeteria`. Prefix: `caf_*` (21 tables). 16 controllers, 6 services (MenuService, OrderService, MealCardService, PosService, StockService, ReportService), ~77 routes (62 web + 15 API), 16 FormRequests, 14 policies, ~50 views. Key: ALL caf_* PKs INT UNSIGNED (not BIGINT); atomic balance deduction via SELECT...FOR UPDATE; Razorpay webhook idempotent (razorpay_payment_id UNIQUE, outside auth); HST bridge (auto mess enrollment on hostel admission); INV bridge (optional PR on stock reorder); SimpleSoftwareIO/simple-qrcode; DomPDF (kitchen sheet, card statement, FSSAI audit). Prompt: `5-Work-In-Progress/Cafeteria/1-Claude_Prompt/CAF_2step_Prompt1.md`.
- [x] **ParentPortal / PPT** (0% code, module not created) — Prompt ready 2026-03-26. `Modules\ParentPortal`. Prefix: `ppt_*` (6 tables). 16 controllers, 5 services (ParentDashboardService, FeePaymentService, MessagingService, NotificationPreferenceService, PtmSchedulingService), 9 FormRequests, 3 policies, custom `parent.portal` middleware, ~75 web + ~18 API routes (~93 total), ~45 views, 38 screens. P0: ParentChildPolicy on every data endpoint (IDOR prevention); parent.portal middleware returns 404 not 403; active_student_id stored in DB for multi-device sync; OTP max 3/hr + max 3 attempts + 30-min lockout; Razorpay payment_reference UNIQUE nullable (idempotency). Phase 2 = DDL (6 tables) + Authorization Architecture (middleware + policies + FormRequests + service skeletons). ~21 person-days to build (P0:2 + P1:5 + P2:5 + P3:4 + architecture:5). Prompt: `5-Work-In-Progress/ParentPortal/1-Claude_Prompt/PPT_2step_Prompt1.md`.
- [x] **Maintenance / MNT** (0% code, module not created) — Prompt ready 2026-03-27. `Modules\Maintenance`. Prefix: `mnt_*` (11 tables). 9 controllers (8 web + 1 mobile API), 5 services (TicketService, AssignmentService, PmScheduleService, EscalationService, DepreciationService), 11 FormRequests, 11 policies, 4 jobs, ~55 web + ~9 API routes, ~35 views, 25 screens. Key: ticket number lock-for-update (BR-MNT-001); 7-state ticket FSM (BR-MNT-003); SLA escalation levels via sla_escalation_json (BR-MNT-011); QR auto-generated on asset save via SimpleSoftwareIO (BR-MNT-015); breakdown history auto-inserted on Resolved (BR-MNT-014); AMC alert idempotency flags (BR-MNT-007); mnt_asset_depreciation + mnt_breakdown_history NO deleted_at (immutable); DomPDF work order PDF; mobile API 9 endpoints. 4 scheduled jobs: GeneratePmWorkOrdersJob (daily 06:00), CheckSlaBreachesJob (every 30 min), SendAmcExpiryAlertsJob (daily 08:00), MarkOverduePmWorkOrdersJob (daily 07:00). 2 seeders: 9 asset categories with keyword JSON. 11 implementation phases. Prompt: `5-Work-In-Progress/Maintenance/1-Claude_Prompt/MNT_2step_Prompt1.md`.
- [x] **Certificate / CRT** (0% code, module not created) — Prompt ready 2026-03-27. `Modules\Certificate`. Prefix: `crt_*` (10 tables). 9 controllers, 3 services (CertificateGenerationService, QrVerificationService, DmsService), 10 FormRequests, 8 policies, 1 job (BulkGenerateCertificatesJob), ~58 web + 2 API routes, ~30 views, 26 screens. Key: HMAC-SHA256 verification_hash on every cert (APP_KEY); QR via SimpleSoftwareIO → no-login public /verify/{hash}; DomPDF PDFs; TC fee-clear gate (BR-CRT-001); TC writes std_students.tc_issued=true (BR-CRT-011 direct write); serial counter SELECT FOR UPDATE (BR-CRT-015); bulk > 200 = queue mandatory (BR-CRT-009); crt_template_versions NO deleted_at (immutable); public verification DTO exposes first name + last initial only (BR-CRT-010). 2 seeders: 5 types + 5 starter templates. 4 implementation phases: P1 (types/templates/DDL), P2 (requests/generation/QR), P3 (TC/bulk/ID cards), P4 (DMS/reports/portal). Prompt: `5-Work-In-Progress/Certificates/1-Claude_Prompt/CRT_2step_Prompt1.md`.
- [x] **VisitorSecurity / VSM** (0% code, module not created) — Prompt ready 2026-03-27. `Modules\VisitorSecurity`. Prefix: `vsm_*` (13 tables in 4 domain groups: Visitor Core, Access Control, Guard Operations, Emergency+CCTV). 9 controllers (8 web + 1 API Report), 4 services (VisitorService, SecurityAlertService, PatrolService, ContractorAccessService), 10 FormRequests, 8 policies, ~70 web + 12 API routes, ~32 views, 25 screens (SCR-VSM-01 to SCR-VSM-25). Key: UUID v4 gate pass tokens (Str::uuid() — never sequential, BR-VSM-002); DB::transaction+lockForUpdate on check-in (BR-VSM-003 race condition); emergency broadcast on dedicated 'emergency' queue (bypasses rate limiting, 3 retries); 4 scheduled jobs (FlagOverdueVisitorsJob every 15 min, ExpireGatePassesJob hourly, ExpireBlacklistEntriesJob daily midnight, ExpireContractorPassesJob daily 00:01); SimpleSoftwareIO/simple-qrcode for gate passes + patrol checkpoints; DomPDF for visitor badges + reports; vsm_patrol_checkpoint_log + vsm_cctv_events NO updated_at/deleted_at (immutable); public gate pass scan URL (no auth); CCTV webhook (no auth + X-CCTV-Secret header); 2 seeders (VsmEmergencyProtocolSeeder + VsmPatrolCheckpointSeeder). All vsm_* PKs BIGINT UNSIGNED. Prompt: `5-Work-In-Progress/VisitorSecurity/1-Claude_Prompt/VSM_2step_Prompt1.md`.

## Pending Modules

- [x] **MarksheetGeneration / MSG** — Full design complete 2026-04-13. `Modules\MarksheetGeneration`. Prefix: `msh_*` (23 tables). 10 controllers, 23 models, 10 services (4 main + 6 score readers), 1 job, 18 FormRequests, 8 policies, ~18 views. 23 tenant migrations. 5 sprints / ~9.5 weeks. Read-only integration with LmsExam, LmsHomework, LmsQuiz, LmsQuest, BehaviouralAssessment. Supports CBSE/ICSE/State Board marksheet formats. Key: subject-wise exam-wise result matrix, theory-practical split, IA marks, co-scholastic grades, rank/division/promotion, DomPDF PDF. **Screen Design Specs (19 screens)** complete with ASCII wireframes, field-to-DDL mapping, validation rules, navigation flow, Livewire components (2). **Open Q-13 (critical):** `lms_exam_papers` has no `is_practical` flag — must resolve before Sprint 3. Output: `{OLD_REPO}/1-DDL_Tenant_Modules/55h-MarksheetGeneration/` (4 core files + 4 design files). Decision: D32. Context: `.ai-contexts/2026-04-13_19-50_marksheet-generation-module-full-design.md`.
- [ ] **Behavioral Assessment** — Student behavior tracking and analysis (DDL ready: `BA_DDL_v2.sql`)
- [ ] **Analytical Reports** — Cross-module analytics and reporting
- [x] **Hostel Management (HST)** — Prompt ready. See "Requirements Complete" above.
- [x] **Mess/Canteen (CAF)** — Prompt ready. See "Requirements Complete" above.
- [x] **Admission Enquiry (ADM)** — Prompt ready. See "Requirements Complete" above.
- [x] **VisitorSecurity (VSM)** — Prompt ready. See "Requirements Complete" above.
- [x] **FrontDesk (FOF)** — Prompt ready. See "Requirements Complete" above.
- [x] **Certificate / CRT** — Prompt ready. See "Requirements Complete" above.
- [x] **Maintenance / MNT** — Prompt ready. See "Requirements Complete" above.
- [ ] **Help Desk** — Support ticket system
- [x] **Student/Parent Portal (STP + PPT)** — Both prompts ready. See "Requirements Complete" above.

---

## Testing Progress

### Infrastructure (done)
- [x] `phpunit.xml` — updated with all module test suites
- [x] `AI_Brain/memory/testing-strategy.md` — full testing strategy documented
- [x] `AI_Brain/agents/test-agent.md` — Pest 4.x patterns and cheatsheet
- [x] `AI_Brain/templates/test-unit.md` — unit test boilerplate
- [x] `AI_Brain/templates/test-feature-central.md` — central feature test boilerplate
- [x] `AI_Brain/templates/test-feature-tenant.md` — tenant feature test boilerplate

### Test File Inventory (audited 2026-03-22)

| Location | Module | Files | What It Covers |
|----------|--------|-------|----------------|
| `tests/Unit/Central/` | Prime | 5 | Setting model (16 unit + 14 DB tests) |
| `tests/Unit/SmartTimetable/` | SmartTimetable | 6 | TimetableSolution::isPlaced, parallel group backtrack |
| `tests/Unit/Hpc/` | Hpc | 6 | HpcWorkflow, SectionRole, Report, Attendance, Credit, DataMapping services |
| `tests/Unit/StudentProfile/` | StudentProfile | 1 | Student model |
| `tests/Feature/SmartTimetable/` | SmartTimetable | 1 | ConstraintManager feature test |
| `tests/Feature/Hpc/` | Hpc | 1 | HPC authorization test |
| `tests/Browser/Modules/Class&SubjectMgmt/` | SchoolSetup | 9 | Browser CRUD tests |
| `tests/Browser/Modules/Complaint/` | Complaint | 4 | Browser CRUD tests |
| `tests/Browser/Modules/HPC/` | Hpc | 1 | HPC parameters CRUD |
| `tests/Browser/Modules/Library/` | Library | 15 | Browser CRUD tests |
| `tests/Browser/Modules/StudentProfile/` | StudentProfile | 5 | Browser CRUD tests |
| `Modules/Accounting/tests/` | Accounting | 14 | Module-level tests |
| `Modules/StudentFee/tests/` | StudentFee | 24 | Module-level tests |
| `Modules/Prime/tests/` | Prime | 9 | Module-level tests |
| `Modules/TimetableFoundation/tests/` | TimetableFoundation | 7 | Module-level tests |
| `Modules/Payment/tests/` | Payment | 8 | Module-level tests |
| `Modules/StudentPortal/tests/` | StudentPortal | 7 | Module-level tests |
| `Modules/GlobalMaster/tests/` | GlobalMaster | 4 | Module-level tests |
| **Other module tests/** | Various | 1 each | Billing, Documentation, Scheduler, SystemConfig (minimal) |

**Total: 134 test files across the project.**
**Modules with 0 test files:** SchoolSetup, Transport, Syllabus, QuestionBank, LmsExam, LmsQuiz, LmsHomework, LmsQuests, Notification, Vendor, Recommendation, SyllabusBooks, SmartTimetable (module-level), Hpc (module-level), StandardTimetable, Dashboard.

---

## Platform-Wide P0 Security Issues (from Gap Analysis 2026-03-22)

1. **ROTATE** QuestionBank API keys (OpenAI `sk-proj-...` + Gemini `AIzaSyD-...`) — exposed in source
2. Remove `is_super_admin` from User `$fillable` (SchoolSetup UserController, StudentProfile StudentController)
3. Remove `dd()` from Complaint and LmsExam production code
4. Fix Payment webhook — move route outside `auth` middleware
5. Remove StudentFee seeder route (`GET /student-fee/seeder`)
6. Add `EnsureTenantHasModule` to all 30 module route groups (currently only 1 usage)
7. Fix StudentPortal IDOR on invoice/payment endpoints
