# Webhook Processing — Test Case List

## Module: Payment (Prefix: `ppt_`)
## Feature: Webhook Processing

---

| # | TC ID | Feature | Test Title | Preconditions | Test Data | Steps | Expected Result | Status | CR | Remarks |
|---|-------|---------|------------|---------------|-----------|-------|-----------------|--------|----|---------|
| 1 | PPT-F4-TC1 | Webhook | Valid Razorpay webhook — payment.captured processed successfully | 1. Active Razorpay gateway configured with `webhook_secret`<br>2. Payment exists with `gateway_order_id` matching payload<br>3. Payment status = "pending" | Valid Razorpay webhook payload with `event: "payment.captured"`, `payload.payment.entity` containing `order_id`, `id`, `signature` | 1. Compute HMAC-SHA256 signature using webhook_secret<br>2. POST `/payment/webhooks/razorpay` with raw JSON body and `X-Razorpay-Signature` header<br>3. Assert HTTP 200, `{status: 'queued'}`<br>4. Assert ptm_payment_webhooks record created with gateway=razorpay, event_type="payment.captured", signature_valid=1, processed=0<br>5. Process the queued job<br>6. Assert job executes successfully<br>7. Assert webhook processed=1<br>8. Assert payment status = "success"<br>9. Assert gateway_payment_id and gateway_signature updated<br>10. Assert audit log for `webhook.payment.captured.received` | HTTP 200. Webhook stored, job processes successfully. Payment marked success. Audit logged. | ⬜ | ◌ | |
| 2 | PPT-F4-TC2 | Webhook | Valid Razorpay webhook — payment.failed processed | 1. Active Razorpay gateway configured<br>2. Payment exists with matching `gateway_order_id`, status="pending" | Razorpay payload with `event: "payment.failed"`, `payload.payment.entity` with `order_id`, `error_description: "Payment declined"` | POST with valid signature, process job | HTTP 200. Payment marked failed with reason "Payment declined". Webhook processed=1. | ⬜ | ◌ | |
| 3 | PPT-F4-TC3 | Webhook | Valid Razorpay webhook — refund.processed | 1. Active Razorpay gateway configured<br>2. PaymentRefund exists with `gateway_refund_id` matching payload | Razorpay payload with `event: "refund.processed"`, `payload.refund.entity` with `id` | POST with valid signature, process job | HTTP 200. Refund marked success. If fully refunded, payment status changes to "refunded". | ⬜ | ◌ | |
| 4 | PPT-F4-TC4 | Webhook | Razorpay webhook — payment.captured with non-existent order_id | 1. Active Razorpay gateway<br>2. No payment with the gateway_order_id in payload | Payload with unknown order_id | POST valid webhook, process job | HTTP 200. Webhook stored. Job processes but silently returns (no matching payment → no-op). Webhook processed=1. | ⬜ | ◌ | handlePaymentCaptured returns early when no payment found |
| 5 | PPT-F4-TC5 | Webhook | Razorpay webhook — payment.failed without order_id | Any Razorpay gateway | Payload without `payload.payment.entity.order_id` | POST valid webhook, process job | HTTP 200. Job processes but handlePaymentFailed returns early. Webhook processed=1. | ⬜ | ◌ | |
| 6 | PPT-F4-TC6 | Webhook | Razorpay webhook — refund.processed with non-existent refund_id | Any Razorpay gateway | Payload with unknown refund entity ID | POST valid webhook, process job | HTTP 200. Job processes but handleRefundProcessed returns early (no matching PaymentRefund). Webhook processed=1. | ⬜ | ◌ | |
| 7 | PPT-F4-TC7 | Webhook | Razorpay webhook — unsupported event type | Any Razorpay gateway | Payload with `event: "payment.authorized"` (not supported) | POST valid webhook, process job | HTTP 200. Job processes. match default case → null (no-op). Webhook processed=1. | ⬜ | ◌ | |
| 8 | PPT-F4-TC8 | Webhook | Valid Paytm webhook — TXN_SUCCESS | 1. Active Paytm gateway configured with `merchant_key`<br>2. Payment exists with matching ULID, status="pending" | Payload with `ORDERID: "{ulid}"`, `STATUS: "TXN_SUCCESS"`, `TXNID: "paytm_txn_123"`, valid CHECKSUMHASH | 1. Compute Paytm checksum<br>2. POST with valid checksum<br>3. Assert HTTP 200<br>4. Process job<br>5. Assert payment status = "success" | HTTP 200. Payment marked success. TXNID stored as gateway_payment_id. | ⬜ | ◌ | |
| 9 | PPT-F4-TC9 | Webhook | Valid Paytm webhook — TXN_FAILURE | Same as TC8 | `STATUS: "TXN_FAILURE"`, `RESPMSG: "Payment failed"` | POST with valid checksum, process job | HTTP 200. Payment marked failed with reason from RESPMSG. | ⬜ | ◌ | |
| 10 | PPT-F4-TC10 | Webhook | Paytm webhook — PENDING status | Same as TC8 | `STATUS: "PENDING"`, `RESPMSG: "Awaiting confirmation"` | POST, process job | HTTP 200. Payment marked failed with reason. (Note: PENDING is mapped to markFailed, not left as pending.) | ⬜ | ◌ | Potential issue: PENDING status treated as failure |
| 11 | PPT-F4-TC11 | Webhook | Paytm webhook — missing ORDERID | Same as TC8 | Payload without ORDERID field | POST, process job | HTTP 200. Job processes but handleWebhook returns early (no ORDERID). Webhook processed=1. | ⬜ | ◌ | |
| 12 | PPT-F4-TC12 | Webhook | Valid PhonePe webhook — PAYMENT_SUCCESS | 1. Active PhonePe gateway configured with `salt_key`<br>2. Payment exists with matching ULID (merchantTransactionId), status="pending" | Payload with `data.merchantTransactionId: "{ulid}"`, `code: "PAYMENT_SUCCESS"`, X-VERIFY header | 1. Compute SHA256 signature<br>2. POST with valid X-VERIFY header<br>3. Assert HTTP 200<br>4. Process job<br>5. Assert payment status = "success" | HTTP 200. Payment marked success. | ⬜ | ◌ | |
| 13 | PPT-F4-TC13 | Webhook | Valid PhonePe webhook — PAYMENT_ERROR | Same as TC12 | `code: "PAYMENT_ERROR"` | POST, process job | HTTP 200. Payment marked failed with reason "PAYMENT_ERROR". | ⬜ | ◌ | |
| 14 | PPT-F4-TC14 | Webhook | Valid PhonePe webhook — PAYMENT_CANCELLED | Same as TC12 | `code: "PAYMENT_CANCELLED"` | POST, process job | HTTP 200. Payment marked failed with reason "PAYMENT_CANCELLED". | ⬜ | ◌ | |
| 15 | PPT-F4-TC15 | Webhook | Valid PhonePe webhook — TIMED_OUT | Same as TC12 | `code: "TIMED_OUT"` | POST, process job | HTTP 200. Payment marked failed with reason "TIMED_OUT". | ⬜ | ◌ | |
| 16 | PPT-F4-TC16 | Webhook | PhonePe webhook — missing merchantTransactionId | Same as TC12 | Payload without merchantTransactionId | POST, process job | HTTP 200. Job processes but returns early. Webhook processed=1. | ⬜ | ◌ | |
| 17 | PPT-F4-TC17 | Webhook | Valid BillDesk webhook — status 0300 (success) | 1. Active BillDesk gateway configured with `checksum_key`<br>2. Payment exists with matching ULID, status="pending" | `BD_RESPONSE`: pipe-separated string with field[1]=ULID, field[14]="0300", valid HMAC checksum | 1. Compute HMAC signature<br>2. POST with valid payload<br>3. Assert HTTP 200<br>4. Process job<br>5. Assert payment status = "success" | HTTP 200. Payment marked success. | ⬜ | ◌ | |
| 18 | PPT-F4-TC18 | Webhook | Valid BillDesk webhook — status 0002 (failed) | Same as TC17 | `BD_RESPONSE` with field[14]="0002" | POST, process job | HTTP 200. Payment marked failed with reason "BillDesk status: 0002". | ⬜ | ◌ | |
| 19 | PPT-F4-TC19 | Webhook | Valid BillDesk webhook — status 0001 (pending) | Same as TC17 | `BD_RESPONSE` with field[14]="0001" | POST, process job | HTTP 200. Payment marked failed (pending treated as failure). | ⬜ | ◌ | Potential issue: 0001 mapped to failure |
| 20 | PPT-F4-TC20 | Webhook | BillDesk webhook — empty BD_RESPONSE | Same as TC17 | Payload without BD_RESPONSE | POST, process job | HTTP 200. Job processes but returns early. Webhook processed=1. | ⬜ | ◌ | |
| 21 | PPT-F4-TC21 | Webhook | CCAvenue webhook — no-op processing | 1. Active CCAvenue gateway configured<br>2. Any payload | Any valid payload | POST `/payment/webhooks/ccavenue`, process job | HTTP 200. Webhook stored. Job runs but handleWebhook is no-op. Webhook processed=1. | ⬜ | ◌ | |
| 22 | PPT-F4-TC22 | Webhook | Offline gateway webhook — no-op processing | 1. Active offline gateway<br>2. Any payload | Any valid payload | POST `/payment/webhooks/offline`, process job | HTTP 200. Webhook stored. Job runs but handleWebhook is no-op. Webhook processed=1. | ⬜ | ◌ | |
| 23 | PPT-F4-TC23 | Webhook | Invalid webhook signature — Razorpay | Active Razorpay gateway | Any payload with invalid HMAC signature | POST with invalid X-Razorpay-Signature header | HTTP 401. `{error: 'Invalid signature'}`. No webhook record created. | ⬜ | ◌ | |
| 24 | PPT-F4-TC24 | Webhook | Invalid webhook signature — Paytm | Active Paytm gateway | Payload with invalid CHECKSUMHASH | POST with wrong checksum | HTTP 401. `{error: 'Invalid signature'}`. | ⬜ | ◌ | |
| 25 | PPT-F4-TC25 | Webhook | Invalid webhook signature — PhonePe | Active PhonePe gateway | Payload with invalid X-VERIFY header | POST with wrong X-VERIFY | HTTP 401. `{error: 'Invalid signature'}`. | ⬜ | ◌ | |
| 26 | PPT-F4-TC26 | Webhook | Invalid webhook signature — BillDesk | Active BillDesk gateway | Payload with invalid HMAC in BD_RESPONSE | POST with wrong checksum | HTTP 401. `{error: 'Invalid signature'}`. | ⬜ | ◌ | |
| 27 | PPT-F4-TC27 | Webhook | Empty request body | Any active gateway | Empty body | POST with empty body | HTTP 400. `{error: 'Empty body'}`. | ⬜ | ◌ | |
| 28 | PPT-F4-TC28 | Webhook | Invalid JSON body | Any active gateway | Malformed JSON: `{invalid: ` | POST with malformed JSON | HTTP 400. `{error: 'Invalid JSON'}`. | ⬜ | ◌ | |
| 29 | PPT-F4-TC29 | Webhook | Missing gateway route parameter | — | Valid body, no gateway in URL | POST `/payment/webhooks/` (no gateway) | HTTP 400. `{error: 'Missing gateway'}`. | ⬜ | ◌ | |
| 30 | PPT-F4-TC30 | Webhook | Non-existent gateway code | — | Valid body, gateway="nonexistent" | POST `/payment/webhooks/nonexistent` | HTTP 500. "Payment gateway [nonexistent] not found or inactive." | ⬜ | ◌ | |
| 31 | PPT-F4-TC31 | Webhook | Inactive gateway code | Gateway exists but is_active=0 | Valid body, inactive gateway code | POST with inactive gateway | HTTP 500. "Payment gateway [code] not found or inactive." | ⬜ | ◌ | |
| 32 | PPT-F4-TC32 | Webhook | Idempotency — duplicate idempotency_key | First webhook already stored with same idempotency_key | Same payload as previously received (generates same idempotency_key) | POST same webhook again | HTTP 200 (first webhook stored OK). Second POST → DB UNIQUE constraint violation. **Current behavior:** Likely leads to 500 error from DB exception. (Improvement area: catch UNIQUE and return 200 gracefully.) | ⬜ | ◌ | Evaluate: should gracefully handle duplicate |
| 33 | PPT-F4-TC33 | Webhook | Idempotency — different payloads, same gateway, different idempotency | Two different webhook payloads from same gateway | Two different event payloads | POST both | Both stored successfully with different idempotency keys. | ⬜ | ◌ | |
| 34 | PPT-F4-TC34 | Webhook | Job failure — driver handleWebhook throws exception | 1. Active gateway configured<br>2. Webhook stored, job dispatched<br>3. handleWebhook throws \RuntimeException | Payload that causes driver error | 1. POST valid webhook<br>2. Allow job to fail<br>3. Check retry behavior | Job retried 3 times with 30s backoff. On each failure: webhook.error_message set, processed=0. After 3 retries: failed() called, error_message = "Max retries exceeded: {message}". | ⬜ | ◌ | |
| 35 | PPT-F4-TC35 | Webhook | Job retry — successful on second attempt | First attempt fails, second succeeds | Payload causing transient failure | 1. POST webhook<br>2. First job run fails<br>3. Retry succeeds<br>4. Assert webhook processed=1, error_message=null | Webhook eventually processed successfully on retry. | ⬜ | ◌ | |
| 36 | PPT-F4-TC36 | Webhook | Webhook record created with correct defaults | Any valid gateway and payload | Standard valid payload | POST valid webhook, check DB record | ptm_payment_webhooks row: signature_valid=1, processed=0, is_active=1, payment_id=null (if not resolved yet by job), created_at set. | ⬜ | ◌ | |
| 37 | PPT-F4-TC37 | Webhook | WebhookReceived event dispatched on success | Valid webhook that processes successfully | Standard valid payload | 1. POST valid webhook<br>2. Process job successfully<br>3. Assert WebhookReceived event dispatched | WebhookReceived event dispatched with the webhook model. | ⬜ | ◌ | |
| 38 | PPT-F4-TC38 | Webhook | WebhookReceivedListener — logs audit when payment linked | Webhook has linked payment after processing | Payload that resolves to a valid payment | Process job, check audit | Audit log entry with event="webhook.received", actor_type="webhook", payload containing webhook_id, gateway, event_type, processed. | ⬜ | ◌ | |
| 39 | PPT-F4-TC39 | Webhook | WebhookReceivedListener — no audit when payment not linked | Webhook has no linked payment | Payload for non-existent payment | Process job, listener runs | Listener checks `$payment = $webhook->payment; if (!$payment) return;` — no audit logged. | ⬜ | ◌ | |
| 40 | PPT-F4-TC40 | Webhook | Razorpay webhook — payment.captured without signature in payload | Valid Razorpay gateway, matching payment | `payload.payment.entity` without signature field | POST, process job | HTTP 200. markSuccess called with signature=null. Gateway_signature set to null. | ⬜ | ◌ | |
| 41 | PPT-F4-TC41 | Webhook | Backend — list webhooks | 3+ webhooks stored in DB | User with `tenant.payment.reconciliation.viewAny` permission | GET `/admin/payment/webhooks` | HTTP 200. Paginated list of webhooks (20 per page) with payment relation loaded. Shows gateway, event_type, signature_valid, processed columns. | ⬜ | ◌ | |
| 42 | PPT-F4-TC42 | Webhook | Backend — list webhooks unauthorized | User without `tenant.payment.reconciliation.viewAny` permission | — | GET `/admin/payment/webhooks` | HTTP 403. | ⬜ | ◌ | |
| 43 | PPT-F4-TC43 | Webhook | Razorpay webhook — missing webhook_secret credential | Razorpay gateway configured without `webhook_secret` | Standard payload | POST with valid HMAC (secret is empty) | HTTP 401. `verifyWebhookSignature` returns false when secret empty. | ⬜ | ◌ | |
| 44 | PPT-F4-TC44 | Webhook | PhonePe webhook — missing salt_key credential | PhonePe gateway without `salt_key` | Standard payload | POST with valid X-VERIFY | HTTP 401. `verifyWebhookSignature` returns false when salt_key empty. | ⬜ | ◌ | |
| 45 | PPT-F4-TC45 | Webhook | BillDesk webhook — missing checksum_key credential | BillDesk gateway without `checksum_key` | Standard payload | POST with valid HMAC | HTTP 401. `verifyWebhookSignature` returns false when checksum_key empty. | ⬜ | ◌ | |
| 46 | PPT-F4-TC46 | Webhook | Razorpay webhook — X-Razorpay-Signature header missing | Active Razorpay gateway | Valid payload without signature header | POST without X-Razorpay-Signature | HTTP 401. `{error: 'Invalid signature'}`. | ⬜ | ◌ | |
| 47 | PPT-F4-TC47 | Webhook | PhonePe webhook — X-VERIFY header missing | Active PhonePe gateway | Valid payload without X-VERIFY | POST without X-VERIFY | HTTP 401. `{error: 'Invalid signature'}`. | ⬜ | ◌ | |
| 48 | PPT-F4-TC48 | Webhook | Paytm webhook — CHECKSUMHASH missing in payload | Active Paytm gateway | Payload without CHECKSUMHASH | POST without checksum | HTTP 401. `{error: 'Invalid signature'}`. | ⬜ | ◌ | |
| 49 | PPT-F4-TC49 | Webhook | BillDesk webhook — empty BD_RESPONSE in payload | Active BillDesk gateway | Payload with BD_RESPONSE="" | POST with empty BD_RESPONSE | HTTP 401. `{error: 'Invalid signature'}`. | ⬜ | ◌ | |
| 50 | PPT-F4-TC50 | Webhook | Gateway driver throws during resolve in middleware | Gateway code valid but driver class broken | Any payload | POST webhook | HTTP 500. `{error: 'Gateway error'}`. | ⬜ | ◌ | |
| 51 | PPT-F4-TC51 | Webhook | Paytm webhook — PENDING status from gateway but should not be failure | Payment in pending state | Payload with STATUS="PENDING" | POST, process job | Payment marked failed. **Evaluation required:** Should PENDING webhooks leave payment as pending instead of marking failed? | ⬜ | ◌ | Behavior question: PENDING→markFailed may be incorrect |
| 52 | PPT-F4-TC52 | Webhook | BillDesk webhook — unknown status code | Active BillDesk gateway | `BD_RESPONSE` with field[14]="9999" (unknown) | POST, process job | HTTP 200. Job processes. Falls through to markFailed (not in [0300] success condition). Payment marked failed with "BillDesk status: 9999". | ⬜ | ◌ | |
| 53 | PPT-F4-TC53 | Webhook | Razorpay webhook — large payload stress test | Active Razorpay gateway | Payload with large payload.payment.entity (many nested fields) | POST with large payload | HTTP 200. Webhook stored successfully in JSON column. | ⬜ | ◌ | Check MySQL max_allowed_packet limits |
| 54 | PPT-F4-TC54 | Webhook | Gateway code case sensitivity | Gateway configured with code "Razorpay" (uppercase R) | URL with "razorpay" (lowercase) | POST `/payment/webhooks/razorpay` | Match fails if gateway code is case-sensitive. **Check:** GatewayManager does exact match on code. | ⬜ | ◌ | Verify gateway code normalization |
| 55 | PPT-F4-TC55 | Webhook | VerifyWebhookMiddleware — raw body preserved for signature | Any gateway | JSON payload with special characters (non-ASCII) | 1. POST with special chars<br>2. Verify middleware reads `$request->getContent()` before JSON decode | Signature verified against raw body, not re-encoded JSON. | ⬜ | ◌ | Important for HMAC integrity |
| 56 | PPT-F4-TC56 | Webhook | Webhook processed but payment already in final state | Payment already status="success" when webhook arrives | Duplicate payment.captured webhook | POST duplicate webhook, process job | HTTP 200. Job runs. markSuccess called on already-success payment — updates gateway_payment_id again, paid_at updated. | ⬜ | ◌ | |
| 57 | PPT-F4-TC57 | Webhook | Webhook for gateway with no webhook support (offline) | Active offline gateway | Any payload | POST `/payment/webhooks/offline` | HTTP 200 (offline verifyWebhookSignature returns true). Job processes (no-op). Webhook stored but meaningless. | ⬜ | ◌ | Improvement: consider blocking webhooks for offline gateways at route level |
| 58 | PPT-F4-TC58 | Webhook | List webhooks — empty state | No webhooks in DB | User with permission | GET `/admin/payment/webhooks` | HTTP 200. Empty paginated result. | ⬜ | ◌ | |
| 59 | PPT-F4-TC59 | Webhook | ProcessWebhookJob — serialization of PaymentWebhook model | Job serialized/deserialized on queue | Standard webhook | 1. Dispatch job to queue<br>2. Queue worker deserializes | Job correctly deserializes PaymentWebhook model with loaded relations. | ⬜ | ◌ | |
| 60 | PPT-F4-TC60 | Webhook | Webhook with null idempotency key (should not happen) | Gateway extraction returns null | Payload where all extraction strategies return null | POST webhook | Fallback to `{gateway}_{uniqid}` ensures non-null key. Webhook stored. | ⬜ | ◌ | |

---

## Legend

| Symbol | Meaning |
|--------|---------|
| V1/V2 | — = Not versioned |
| Status | ⬜ = Not Executed, ✅ = Passed, ❌ = Failed, ⛔ = Blocked |
| CR | ◌ = No Change Request, 🔄 = Change Requested |

---

## Change Log

| V1/V2 | Status | CR | Date | Description |
|-------|--------|----|------|-------------|
| — | ⬜ | ◌ | 2026-07-23 | Initial test case list generated from code analysis |
