# D12 — Documentation & Handover

> **Audit Date:** 2026-07-02 | **Mode:** READ-ONLY production-readiness audit
> **App:** `/Users/bkwork/Herd/prime_ai` | **Docs repo:** `/Users/bkwork/WorkFolder/1-Old_PrimeDB/old_db`

---

## 1. Findings Table

| ID | Sev | Area | Description | Evidence | Remediation | Effort |
|---|---|---|---|---|---|---|
| GAP-D12-001 | **P0** | Secrets in repo | **`.env-original` is git-tracked at the app root and contains a non-empty `APP_KEY`, `REDIS_PASSWORD` and `MAIL_PASSWORD`.** A committed APP_KEY compromises every Laravel `encrypted` cast and signed cookie/session — it directly invalidates the D11 encryption remediation until rotated. | `cd /Users/bkwork/Herd/prime_ai && git ls-files` includes `.env-original`; file lines 3 (`APP_KEY=` non-empty), 51 (`REDIS_PASSWORD=` non-empty), 59 (`MAIL_PASSWORD=` non-empty) | Remove from index, add to `.gitignore`, **rotate APP_KEY + mail/redis creds**, purge from git history (filter-repo), re-encrypt any `encrypted`-cast data under new key | 1-2 d |
| GAP-D12-002 | **P1** | Credentials doc committed | `TENANT_ADMIN_CREDENTIALS.md` is git-tracked at repo root with plaintext tenant admin login (`root@tenant.com` / `password` for `test.localhost`). The file's own footer says "do not commit it to version control" — yet it is committed. **Confirms known baseline.** | `/Users/bkwork/Herd/prime_ai/TENANT_ADMIN_CREDENTIALS.md:1-12`; present in `git ls-files` | Delete from repo + history; ensure the seeded default password is changed/disabled in production tenants | 0.5 d |
| GAP-D12-003 | P2 | Admin onboarding doc | **A new admin cannot create a school end-to-end from existing docs.** `README.md` is a near-stock single-app Laravel setup guide (composer/npm/`php artisan serve`, last updated 2025-10-08 by Tarun Chauhan). It contains **zero** multi-tenancy content: no central `prime_db`/`global_db` setup, no `tenants:migrate`, no queue/scheduler/Redis requirements, no domain (`prm_tenant_domains`) config, no school-creation walkthrough. What the README does claim is accurate (Laravel 12 → `composer.json` `"laravel/framework": "^12.0"`; spatie permission/medialibrary/backup present) — it is incomplete, not wrong. Docs repo only offers author-machine-specific local run notes. | `/Users/bkwork/Herd/prime_ai/README.md` (Setup Instructions section); `composer.json:` laravel/framework ^12.0; `old_db/8-How_Tos/Run_Local_WebApp_MobileApp/Run_WebApp_MobileApp.md` (hardcoded `/Users/bkwork/Herd`, `192.168.29.100`) | Write an "Operator Guide": provision central DBs → seed plans/modules → create tenant + domain → run tenant migrations/seeders → first-admin login → module activation | 3-4 d |
| GAP-D12-004 | P2 | API documentation | Real external API surface exists but is **entirely undocumented**: `routes/api.php` defines a mobile API (`mobile/v1` — `X-App-Key` via `mobile.key`, tenant resolution via `tenant.mobile`, Bearer via `auth:sanctum`), plus 40+ module-level `routes/api.php` files under `auth:sanctum`/`v1`. No OpenAPI/Scribe/Swagger package in composer (`grep scribe|l5-swagger|dedoc` → none), no API docs in `docs/` or old_db. Mobile-app developers must read middleware source to learn the header contract. | `/Users/bkwork/Herd/prime_ai/routes/api.php:9-30`; `Modules/*/routes/api.php` (e.g. Accounting, Admission, Certificate…); `composer.json` `laravel/sanctum ^4.0`, no doc generator | Adopt Scribe or hand-written OpenAPI for `mobile/v1` first (auth handshake, headers, error codes), then module APIs | 2-3 d |
| GAP-D12-005 | P2 | Architecture doc drift | `old_db/AI_Brain/memory/architecture.md` (last updated 2026-03-12) is stale on 2 of 3 spot-checked claims: (1) "29 Feature Modules" — actual **45** (`ls Modules | wc -l`); (2) "tenant_{uuid} (368 tables)" — actual **661** create-table migrations in `database/migrations/tenant`; (3) "External Services: Razorpay API (payments)" — actual **6 gateways** (`Razorpay, BillDesk, CCAvenue, Paytm, PhonePe, Offline` in `Modules/Payment/app/Gateways/`). Verified-accurate claim: middleware chain names (`EnsureTenantIsActive`, `EnsureTenantHasModule`, sanctum) all exist in `app/Http/Middleware/`. | `architecture.md` lines ~25-38; `Modules/` count = 45; tenant `create_` migrations = 661; `Modules/Payment/app/Gateways/` listing | Refresh counts + external-services section; add "generated on" automation or a doc-refresh checklist item per release | 1 d |
| GAP-D12-006 | P2 | Ops / handover docs | No deployment runbook, upgrade notes, backup/restore procedure, or incident playbook in either repo. App `docs/` contains only `superpowers/plans` (9 historical dev-planning notes, Apr 2026) — nothing operational. spatie/laravel-backup is installed per README but its restore procedure is undocumented. Old_db how-tos are Claude-usage and local-dev guides, not production ops. | `ls /Users/bkwork/Herd/prime_ai/docs` → `superpowers/{plans,specs}` only; `old_db/8-How_Tos/` listing (Claude account switching, agent usage, local run) | Minimum viable runbook: deploy steps, `.env` reference, queue/scheduler supervision, backup verify/restore, tenant provisioning, rollback | 2-3 d |
| GAP-D12-007 | P3 | Internal artifacts in production repo | Dev/AI-internal files are git-tracked in the app repo and would ship in the production artifact: `CLAUDE.md`, `TIMETABLE_GENERATOR_EXPLAINED.md` (588 lines), `Modules/SmartTimetable/DOCS/*` (4 internal analysis docs), `agent-test-reports/` + `docs/superpowers/` (27 tracked files), `log_bug.sh`, `query.sh`, `run_lda_learn.sh`, `perfect_timetable_demo.php`, `perfect_timetable_demo.sql`, stale route backup `routes/web_12_01_2026.php`, and a stray shell-accident file literally named `origin)`. None are secrets (except covered above) but they leak internal process, bloat deploys, and `perfect_timetable_demo.php` at webroot-adjacent root is executable code outside app structure. | `git ls-files` matches listed above from `/Users/bkwork/Herd/prime_ai` | Move internal docs to old_db docs repo; delete `origin)`, demo scripts and dated route backup; add `.gitignore`/deploy-exclude rules | 0.5-1 d |
| GAP-D12-008 | P3 | Docs-repo cred hygiene | `old_db/8-How_Tos/Run_Local_WebApp_MobileApp/Z-Server_cred.md` — a file explicitly named "server cred" in the shared docs repo. Contents inspected are local-network run notes (192.168.29.x, expo start), low sensitivity today, but the naming pattern invites real credentials being added later. | file at path above | Rename/split: run-notes vs credentials; keep creds in a secrets manager, never in the docs repo | 0.5 d |

## 2. Task-by-Task Notes

- **Task 6 (onboarding):** README accuracy verified — stack claims true, but it documents a generic single-tenant Laravel install; nothing on tenancy, so end-to-end school creation is **not achievable from docs** (GAP-D12-003).
- **Task 7 (API):** surface exists (`routes/api.php` mobile/v1 + per-module `v1` sanctum routes); documentation: **no evidence found in repo** (GAP-D12-004).
- **Task 8 (architecture spot-check):** 3 claims checked → 2 stale (module count, table count / payment providers), 1 accurate (middleware chain) (GAP-D12-005). Upgrade notes: **no evidence found in either repo** (GAP-D12-006).
- **Task 9 (internal docs at root):** confirmed tracked and flagged for removal (GAP-D12-001/002/007); worst offenders are `.env-original` (live APP_KEY) and `TENANT_ADMIN_CREDENTIALS.md`.

## 3. Verdict — D12

**NOT PRODUCTION-READY (conditional).** One P0 — a committed `.env-original` carrying a live APP_KEY (plus redis/mail passwords) — requires immediate rotation and history purge before any deploy; the committed tenant-credentials file (P1) compounds it. Beyond secrets, documentation is materially insufficient for handover: no operator onboarding path, no API docs for a live mobile API, stale architecture doc, and no ops runbook (4× P2). These are 1-2 weeks of focused writing/cleanup, not re-engineering.

**Totals: P0 = 1, P1 = 1, P2 = 4, P3 = 2.**
