# Events supported by Laravel

In Laravel, "Events" refers to the framework's implementation of the Observer Pattern, which allows you to decouple various parts of your application. When something happens (an event), your application can "fire" a signal, and any number of "listeners" can catch it and act on it.

As of 2026, with Laravel 12 and Laravel 13 in play, the ecosystem supports several categories of events.

